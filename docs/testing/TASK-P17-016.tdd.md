# TASK-P17-016 Phase 17 Closeout Evidence

Status: COMPLETE / CODE BASELINE PASS / USER ACCEPTED WITH DEFERRED FOLLOW-UPS

This document records the Phase 17 closeout audit. Planning and classification
are not implementation evidence. Each result must identify whether it came from
a fresh Build, focused Automation, Editor reopen, PIE, user visual observation,
or remains `NOT VERIFIED`.

## Scope

- Audit P17-005 through P17-015 against the Phase 17 execution plan.
- Reproduce the known ScreenLifecycle and CharacterDetail ViewModel test
  boundaries before deciding whether a production fix is required.
- Run the smallest fresh build and focused UI automation set that can support a
  phase-level decision.
- Keep P17-016 separate from Phase 18 presentation work.

## Initial gate

- Gate 0: planning started; exact implementation allowlist is not yet opened.
- Fresh Build: `PASS`.
- Focused Automation: `PASS`.
- Editor/PIE/resolution/controller evidence: inherited or user-owned; not
  reclassified as fresh evidence in this task.

## Failure policy

The first concrete failure and its owning boundary will be recorded verbatim.
No unrelated dirty-worktree file, UAsset, Config, or business Authority will be
modified while reproducing the baseline.

## Fresh baseline (2026-08-10)

- `HSREditor Win64 Development` Build completed with `Result: Succeeded`.
  UHT/Compile/Link/Metadata completed; UE emitted only the known non-blocking
  warning that MSVC 14.51 is not the preferred compiler version.
- `Automation RunTests HSR.UI` completed with exit code 0 and
  `**** TEST COMPLETE. EXIT CODE: 0 ****`.
- The log reports `Result={Success}` for all discovered HSR.UI tests, including
  `HSR.UI.CharacterDetail.ViewModel`, all `HSR.UI.ScreenLifecycle` cases,
  FrontendNavigation, Inventory, Party, PreBattleCandidate, Quest, SaveFrontend,
  RelicEquipment, and related dynamic-mount suites.
- The default sandbox first failed before test execution because the global UBT
  cache file under `C:\Users\Lai\AppData\Local\UnrealEngine` was denied. The
  identical controlled-permission Build/Automation commands passed. This is
  environment evidence, not a project failure.

## Plan classification checkpoint

- **P17-013 Battle HUD:** existing `HSRBattleCommandViewModel`, command widget,
  narrow command sink, target selection and result-confirm contracts are present;
  the current `HSR.UI` run passes their available automation coverage. Final
  phase classification still needs user battle PIE evidence.
- **P17-014 Battle Pause/Restart/Exit/Reward:** settlement confirmation and
  Reward Summary projection exist and are covered by current code paths, but no
  dedicated authoritative Restart/Exit flow or focused test was found in the
  repository audit. This remains `NOT VERIFIED / FOLLOW-UP REQUIRED`.
- **P17-015 Gacha:** no Gacha Authority, economy, pity or authored Gacha asset is
  present. Per the Phase 17 plan this may be closed as `UI CONTRACT PASS /
  BUSINESS BLOCKED`; real draw behavior is explicitly out of scope.

## User closeout decision

- P17-013 is accepted as sufficient for the current playable-demo target.
- P17-014 Battle Pause/Restart/Exit and its remaining Reward Presentation polish
  are explicitly deferred and do not block Phase 17 closeout.
- P17-015 Gacha is explicitly deferred and does not block Phase 17 closeout.
- Physical controller, dual-resolution and any unrerun visual edge cases remain
  `NOT VERIFIED`; they are not rewritten as passed evidence.
- Final task status: `READY WITH DEFERRED FOLLOW-UPS / USER ACCEPTED`.
