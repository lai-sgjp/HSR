# Task Template

> 使用规则：复制本模板覆盖 `tasks/active-task.md`。同一时间只能有一张活动任务卡；所有占位符必须替换，未列入允许清单的文件默认禁止修改。

## Role Lock / 角色锁定

低级执行模型当前且仅能担任 `Implementation Agent / 低级执行模型`。未经用户明确授权或本活动任务卡明确授权，不得切换为高级模型、Prompt Planner、Prompt Reviewer、Architect、Code Reviewer、Safety Reviewer、Learning Reviewer、Teacher、项目经理、架构负责人或其他权限、职责更大的角色。

不得以“自我反思”“为了安全”或“为了更好完成任务”为由扩权；不得自行重写任务目标、扩大文件范围、改变 Phase 或验收标准。发现风险、歧义、上下文缺失、模型不匹配或需要高阶角色时，必须停止并说明：超出权限原因、所需角色、所需用户授权，并承诺获得授权前不修改文件。若获授权切换，必须显式说明触发依据和授权来源；未经授权的角色切换视为任务失败。

## 任务编号

`【TASK-编号】`

## 任务名称

【一个可独立验收的小任务名称】

## 当前 Phase

【Phase 编号、阶段状态与前置门禁】

## 任务目标

【一句话说明唯一目标、成功结果和明确非目标】

## 规划审查摘要

### 1. 子 Agent 分析

【Prompt Planner、Prompt Reviewer、Architect、Safety Reviewer 的结论；只写与本任务有关的决定】

### 2. 是否适合当前 Phase

【前置门禁、相邻性和不能跳过的证据】

### 3. 是否需要拆分

【不需要/已经拆分；列出明确推迟的后续任务】

### 4. 本轮最小任务

【一个可独立验收的最小结果】

### 5. 风险审查

【Blocking、Risk、明确非目标和停止条件】

### 6. 最终低级模型 Prompt

【写成可直接执行的自包含指令，不得依赖聊天隐含上下文】

低级模型首次读取后必须先复述任务编号、计划、文件范围、Editor 操作、验证和风险，最后写“等待用户确认执行 【TASK-编号】。”并停止。只有用户在复述后另发明确确认，才可调用工具或执行任务。

## 执行模型

【高级模型 / 低级模型 / 审查模型 / 用户；说明职责和是否已授权】

## 执行前必须读取的文件

- 【上下文入口】
- 【实现必需文件；低级模型默认只以 active-task.md 作为上下文入口】

## 允许修改文件

- `【精确路径 1】`
- `【精确路径 2】`

未列出的文件默认禁止修改；如需扩展范围，停止并请求用户授权。

## 禁止修改内容

- 【禁止的文件、目录、系统、Phase】
- 【删除、覆盖、批量操作、Git、Config、Blueprint、第三方资源边界】

## 实现步骤

1. 【前置检查】
2. 【最小实现】
3. 【正常路径验证】
4. 【失败路径验证】
5. 【真实状态归档】

## UE Editor 手动操作

1. 【由用户执行的页面、操作、目的和依赖】
2. 【需要回传的日志、截图或现象】

若无 Editor 操作，明确填写“无”。

## 验收标准

- [ ] 【功能/文档结果】
- [ ] 【文件范围】
- [ ] 【编译/测试/PIE/人工验证证据】
- [ ] 【失败路径】
- [ ] 【未验证项已如实记录】

## 审查清单

- [ ] 目标是否只有一个且没有跨 Phase？
- [ ] 允许文件是否为精确路径，实际改动是否越界？
- [ ] 是否遵守 UE 反射、GAS、GC、Tick、UI、DataAsset/Runtime/SaveGame 边界？
- [ ] 是否没有无关重构、删除、覆盖、批量 Config、未授权 Blueprint/资产或 Git 操作？
- [ ] 验证证据是否真实，完成状态是否准确？

## 执行后必须更新的文件

- `tasks/execution-result.md`：低级模型唯一可写的执行报告，记录实际改动、命令/日志、验证、未验证项、阻塞与扩权请求。
- `PROJECT_STATE.md`
- `worklog.md`
- `todo_plan.md`（仅真实进度变化时）
- `learning-journal.md`（仅有可复用知识时）
- 【专项设计/Phase 文档】

> 职责边界：低级模型只能更新 `tasks/execution-result.md`，不得更新本活动任务卡或其余文件。审查模型先结合该报告、实际文件和独立证据得出结论；随后仅由高级模型更新项目状态、日志、todo、学习记录及活动任务卡的归档。

## 执行结果

- 已修改：【待执行模型填写】
- 已验证：【待执行模型填写】
- 未验证：【待执行模型填写】
- Editor 待办：【待执行模型填写】
- 阻塞/扩权请求：【待执行模型填写】
- 审查结论：【待审查模型填写】

## Git 交付

- 角色：【低级执行模型 / 高级审查者 / 高级协调者 / Teacher】
- 固定 commit message：【角色+人格+当前任务与阶段+简短告诉自己做了什么】
- 提交前检查：【status、diff、允许文件、派生产物排除】
- commit hash：【待填写】
- Phase 收尾 push：【Phase 全部子任务完成后填写远端、分支、结果；任务中途不得 push】
