# Task Template

> 使用规则：复制本模板覆盖 `tasks/active-task.md`。同一时间只能有一张活动任务卡；所有占位符必须替换，未列入允许清单的文件默认禁止修改。

## 任务编号

`【TASK-编号】`

## 任务名称

【一个可独立验收的小任务名称】

## 当前 Phase

【Phase 编号、阶段状态与前置门禁】

## 任务目标

【一句话说明唯一目标、成功结果和明确非目标】

## 执行模型

【高级模型 / 低级模型 / 审查模型 / 用户；说明职责和是否已授权】

## 执行前必须读取的文件

- 【上下文入口】
- 【实现必需文件；低级模型默认只以 active-task.md 作为上下文入口】

## 允许修改文件

- `【精确路径 1】`
- `【精确路径 2】`

未列出的文件默认禁止修改；如需扩展范围，停止并请求用户授权。

## 禁止修改内容

- 【禁止的文件、目录、系统、Phase】
- 【删除、覆盖、批量操作、Git、Config、Blueprint、第三方资源边界】

## 实现步骤

1. 【前置检查】
2. 【最小实现】
3. 【正常路径验证】
4. 【失败路径验证】
5. 【真实状态归档】

## UE Editor 手动操作

1. 【由用户执行的页面、操作、目的和依赖】
2. 【需要回传的日志、截图或现象】

若无 Editor 操作，明确填写“无”。

## 验收标准

- [ ] 【功能/文档结果】
- [ ] 【文件范围】
- [ ] 【编译/测试/PIE/人工验证证据】
- [ ] 【失败路径】
- [ ] 【未验证项已如实记录】

## 审查清单

- [ ] 目标是否只有一个且没有跨 Phase？
- [ ] 允许文件是否为精确路径，实际改动是否越界？
- [ ] 是否遵守 UE 反射、GAS、GC、Tick、UI、DataAsset/Runtime/SaveGame 边界？
- [ ] 是否没有无关重构、删除、覆盖、批量 Config、未授权 Blueprint/资产或 Git 操作？
- [ ] 验证证据是否真实，完成状态是否准确？

## 执行后必须更新的文件

- `PROJECT_STATE.md`
- `worklog.md`
- `todo_plan.md`（仅真实进度变化时）
- `learning-journal.md`（仅有可复用知识时）
- 【专项设计/Phase 文档】

## 执行结果

- 已修改：【待执行模型填写】
- 已验证：【待执行模型填写】
- 未验证：【待执行模型填写】
- Editor 待办：【待执行模型填写】
- 阻塞/扩权请求：【待执行模型填写】
- 审查结论：【待审查模型填写】
