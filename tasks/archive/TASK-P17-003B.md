# TASK-P17-003B — Exploration Inventory Screen Stack Integration

Status: `PASS WITH FOLLOW-UP / ARCHIVED`

The authoritative task contract remains in `tasks/active-task.md` at completion time. Engineering Build and Automation passed independent review. User Editor configured the HUD class, I-key forwarding, Back/preferred focus, persisted assets after restart, passed normal and missing-class PIE, then verified reward success, supported-map return, HUD rebuild, visible Reward Summary receipt and repeated Inventory cycles.

Follow-up: reward generation while Inventory is already open is `NOT VERIFIED` because the current graybox UIOnly flow provides no external reward trigger. The legacy unregistered `Map_Phase1_Exploration` is outside the supported MapSubsystem return contract; use `Map_Exploration_P15_B`.

No Git stage, commit or push was performed.
